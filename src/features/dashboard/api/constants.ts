export const BOARD = (id: string) => `/boards/${id}`;
export const BOARDS = '/boards';
export const BOARDS_MOVE = (id: string) => `/boards/${id}/task-column-move`;
export const COLUMNS = '/taskColumns';
export const TASK = (id: string) => `/tasks/${id}`;
export const TASKS = '/tasks';
